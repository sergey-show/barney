# MCP

**Русский** · [English](mcp.md)

MCP здесь — **локальный stdio-сервер в теле**, не облачный коннектор и не настройки в чужом сервисе.

Barney говорит по MCP `2024-11-05` через stdio: `initialize` → `tools/list` → `tools/call`. При старте серверы сами не поднимаются. Процесс живёт до `mcp_stop` или до выхода Barney.

Если хватает навыка или UI, берите [плагин](plugins.ru.md). MCP — когда нужен внешний процесс, которого в ядре нет.

## Рецепт

Сохраняется как плагин:

```
~/.barney/plugins/<name>/
  plugin.json
  mcp.json
```

`mcp.json`:

```json
{
  "command": "npx",
  "args": ["-y", "@modelcontextprotocol/server-github"],
  "env": {
    "GITHUB_TOKEN": "..."
  }
}
```

`command` + `args` запускаются в **worktree сессии**. Окружение процесса — короткий allowlist (`PATH`, `HOME`, локаль, temp) плюс `env` из этого файла. Облачные ключи процесса Barney **не** пробрасываются, пока их явно не запишут в `env`.

Также находятся:

- файлы в стиле Claude (`.mcp.json` / `mcpServers`) внутри бандла плагина
- старый путь `~/.barney/mcp/<name>/mcp.json`

В Web у такого плагина бейдж **MCP**. Отдельной формы «добавить MCP-сервер» нет.

## Оператор

Положите папку выше или попросите Barney подключить. В сессии агент сделает `mcp_start` и `mcp_call`.

Имя — kebab-case (`github-docs`, `context7`).

## Агент

Barney может подключить MCP **сам** на полном ходе:

| Инструмент | Что делает |
|---|---|
| `mcp_list` | Рецепты на диске и какие серверы уже запущены |
| `mcp_write` | Сохранить `~/.barney/plugins/<name>/mcp.json` |
| `mcp_start` | Поднять процесс, handshake, список инструментов |
| `mcp_call` | Вызвать инструмент на запущенном сервере |
| `mcp_stop` | Погасить процесс; рецепт на диске остаётся |

Типичная петля:

1. `mcp_list` — не плодить рецепт, если он уже есть.
2. `mcp_write` — только если stdio-сервер действительно нужен.
3. `mcp_start` — прочитать список инструментов у сервера.
4. `mcp_call` с `server`, `tool` и JSON `arguments`.
5. `mcp_stop`, когда закончили.

После `mcp_write` MCP закрепляется за экземпляром (`kind: mcp`), в git тела уходит `become: mcp <name>`.

Если ход застрял, **не** выдумывать MCP из промаха. Сменить семейство инструментов и всё равно доставить результат. Новый MCP — решение тела, не обходной путь.

## Ограничения

- Таймаут MCP-запроса: 20 с.
- Вложенный специалист может стартовать и вызывать MCP, но не растит ядро.
- `mcp_write` — не то же самое, что карантин навыка: рецепт можно записать сразу. Если восстановление вида «после этой семьи сработала та» — лучше проверенный навык.

Оглавление: [Документация](README.ru.md) · [Плагины](plugins.ru.md) · [Инструменты](tools.ru.md).
